# Public Supabase settings for the production build (Netlify runs
# `npm run build`, which reads this file). Both values are safe to publish:
# the publishable key only opens what the Row Level Security policies in
# supabase/schema.sql allow (public read, writes for signed-in admins only).
# Never put the "secret" / service_role key here.
VITE_SUPABASE_URL=https://cpstnhyvcnnzmhxeqagt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_tvLZwvmsp0IOzjRVLm50cw_6OUTUA2Z
