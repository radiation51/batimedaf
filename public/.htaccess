<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # Serve existing files as-is; everything else falls through to the SPA.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  RewriteRule . /index.html [L]
</IfModule>
